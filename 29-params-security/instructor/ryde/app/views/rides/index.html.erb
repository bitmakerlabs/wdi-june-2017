<h1>
  <% if @lang == 'fr' %>
    <% if @country == 'canada' %>
      Balade Disponible, eh?
    <% else %>
      Balade Disponible
    <% end %>
  <% else %>
    <% if @country == 'canada' %>
      Available Rydes, eh?
    <% else %>
      Available Rydes
    <% end %>
  <% end %>

  <%= form_for :search, url: rides_search_path do |f| %>
    <%= f.text_field :q %>
    <%= f.submit 'Search' %>
  <% end %>

</h1>
